workflows:
  kididudu-ios:
    name: Kididudu Ultra iOS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Build iOS app
        script: flutter build ios --release --no-codesign
    artifacts:
      - build/ios/iphoneos/Runner.app
    publishing:
      email:
        recipients:
          - your@email.com
